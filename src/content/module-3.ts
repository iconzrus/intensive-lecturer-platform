import type { LectureModule } from './schema';
import { topic } from './module-1';

const MODULE3_LINKS = [
  {
    title: 'Refactoring Guru',
    url: 'https://refactoring.guru/',
    description: 'Понятные объяснения SOLID и паттернов с примерами.',
  },
  {
    title: 'Git Documentation',
    url: 'https://git-scm.com/doc',
    description: 'Официальная документация по командам Git и стратегиям работы с историей.',
  },
  {
    title: 'Maven Guides',
    url: 'https://maven.apache.org/guides/index.html',
    description: 'Официальные материалы по lifecycle, repository и структуре сборки Maven.',
  },
  {
    title: 'Gradle User Manual',
    url: 'https://docs.gradle.org/current/userguide/userguide.html',
    description: 'Официальное руководство по Gradle и сборочным сценариям.',
  },
];

export const module3: LectureModule = {
  id: 'module-3',
  title: 'Module 3: SOLID, Patterns, Git, Maven/Gradle',
  targetDurationMinutes: 60,
  audienceLevel: 'Java interns',
  isAvailable: true,
  summary:
    'SOLID и базовые паттерны проектирования, практический Git для ежедневной работы, Maven/Gradle, этапы сборки и локальное хранение зависимостей.',
  topics: [
    topic({
      id: 'solid-principles',
      title: 'SOLID: пять принципов, которые уменьшают хрупкость кода',
      simpleDefinitionOverride:
        'SOLID — это пять принципов проектирования, которые помогают делать код менее хрупким: чтобы класс отвечал за одно, зависимости были заменяемы, а новые требования не ломали половину системы.',
      quickAnswer:
        'SOLID нужен не “для красивой теории”, а чтобы код было проще менять, тестировать и расширять. На практике это разговор про ответственность классов, контракты, композицию и зависимость от абстракций.',
      explainBrief: [
        'SRP: у класса должна быть одна причина для изменения; если класс и валидирует, и сохраняет, и отправляет email, он быстро становится хрупким.',
        'Простой SRP-пример: `OrderService` оформляет заказ, `OrderRepository` сохраняет, `Notifier` отправляет уведомление; если всё смешать в один класс, любое изменение ломает всё сразу.',
        'OCP: систему стараются расширять через новые реализации и композицию, а не постоянной правкой старого кода в десяти местах.',
        'Простой OCP-пример: новый способ оплаты добавляют новой стратегией `PaymentStrategy`, а не вставляют ещё один `if` в старый сервис.',
        'LSP простыми словами: если код умеет работать с `Base`, он должен так же работать с любым наследником `Base` без сюрпризов.',
        'LSP-антипример: в базовом типе есть `fly()`, а `Penguin extends Bird` бросает исключение в `fly()` — значит такой `Penguin` нельзя безопасно подставить вместо `Bird`.',
        'Как правильно: не заставлять всех птиц "уметь летать". Вынести `fly()` в отдельный контракт (`FlyingBird`/`Flyable`), а `Penguin` оставить в типе без полёта.',
        'ISP: лучше несколько узких интерфейсов, чем один “комбайн”, который заставляет реализовывать лишние методы.',
        'Простой ISP-антипример: интерфейс `ReportActions` с `print/fax/email`, где большинству классов нужны 1-2 метода.',
        'DIP: бизнес-логика зависит от абстракций, а не от конкретных HTTP/DB/SDK клиентов.',
        'Простой DIP-антипример: `OrderService` внутри создаёт `new EmailNotifier()` и жёстко привязан к одной реализации.',
        'Практический способ проверять SOLID: смотреть, сколько причин для изменения у класса, насколько легко подменить зависимость в тесте и не приходится ли править старый код при добавлении нового сценария.',
        'Если после “улучшения архитектуры” стало вдвое больше интерфейсов и файлов, а менять код не стало легче, значит SOLID применили слишком формально.',
        'На собеседовании ценится не расшифровка букв, а умение показать живой пример, где нарушение принципа уже привело к боли.',
      ],
      interviewFocus: [
        {
          question: 'Что такое SOLID и зачем он нужен в реальном проекте?',
          expectedAnswer:
            'SOLID помогает уменьшать связанность и хрупкость кода. Сильный ответ обычно связывает принципы с реальными эффектами: легче тестировать, проще менять поведение, меньше каскадных правок и меньше риска сломать соседние модули.',
        },
        {
          question: 'Какой принцип SOLID чаще всего нарушают начинающие разработчики?',
          expectedAnswer:
            'Часто нарушают SRP и DIP: делают сервисы “на все случаи”, где смешаны бизнес-правила, работа с БД, HTTP и форматирование ответа, а зависимости привязывают к конкретным реализациям.',
        },
        {
          question: 'Как быстро понять на живом коде, что SOLID уже нарушен?',
          expectedAnswer:
            'Типовые маркеры такие: у класса несколько причин для изменения, новый сценарий требует лезть в старый код с if-else, наследник ведёт себя неожиданно, интерфейс заставляет реализовывать лишние методы, а сервис невозможно нормально тестировать без реальной БД или HTTP-клиента.',
        },
      ],
      codeExample: {
        title: 'DIP и SRP на маленьком примере',
        language: 'java',
        snippet: `interface Notifier {
    void send(String message);
}

class EmailNotifier implements Notifier {
    public void send(String message) {
        System.out.println("send email: " + message);
    }
}

class OrderService {
    private final Notifier notifier;

    OrderService(Notifier notifier) {
        this.notifier = notifier;
    }

    public void completeOrder(String orderId) {
        notifier.send("Order completed: " + orderId);
    }
}`,
        walkthrough: [
          'Сервис зависит от абстракции `Notifier`, а не от конкретного email-клиента: это шаг к DIP.',
          'Отправка уведомления вынесена в отдельную ответственность, а не смешана с бизнес-логикой заказа: это шаг к SRP.',
        ],
        commonPitfall: 'Запоминать расшифровки SRP/OCP/LSP/ISP/DIP, но не уметь распознать нарушение принципов в обычном сервисе.',
        productionNote:
          'SOLID не требует дробить код “до атомов”. Если абстракция не даёт пользы, а только усложняет чтение, это уже перегиб.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        {
          term: 'SRP',
          meaning:
            'Single Responsibility Principle: класс должен делать что-то одно. Если один класс и считает цену, и пишет в БД, и шлёт email, его будет больно менять.',
        },
        {
          term: 'OCP',
          meaning:
            'Open/Closed Principle: новый сценарий лучше добавлять через новую реализацию или стратегию, а не переписывать старый код с ещё одним `if/else` в нескольких местах.',
        },
        {
          term: 'LSP',
          meaning:
            'Liskov Substitution Principle: наследник должен вести себя так, чтобы код с базовым типом продолжал работать нормально. Если подставили наследника и всё сломалось или стало вести себя странно, принцип нарушен.',
        },
        {
          term: 'ISP',
          meaning:
            'Interface Segregation Principle: лучше несколько маленьких интерфейсов под задачу, чем один огромный. Класс не должен реализовывать методы, которые ему вообще не нужны.',
        },
        {
          term: 'DIP',
          meaning:
            'Dependency Inversion Principle: сервису лучше зависеть не от конкретного `EmailSender` или `StripeClient`, а от интерфейса вроде `Notifier` или `PaymentGateway`. Тогда реализацию проще заменить и проще тестировать.',
        },
      ],
      estimatedMinutes: 7,
    }),
    topic({
      id: 'design-patterns-basics',
      title: 'Паттерны проектирования: когда они помогают, а когда мешают',
      simpleDefinitionOverride:
        'Паттерн проектирования — это типовой способ решить часто повторяющуюся задачу в архитектуре или коде. Паттерны полезны, когда закрывают реальную проблему, а не когда их вставляют “для солидности”.',
      quickAnswer:
        'На базовом уровне важно понимать не каталог из десятков паттернов, а самые частые: Strategy, Factory Method, Builder, Adapter, Observer, Singleton. Ещё важнее — уметь объяснить, какую проблему каждый из них решает.',
      explainBrief: [
        'Strategy помогает менять алгоритм или правило без if-else на полэкрана.',
        'Factory скрывает создание объектов и помогает выбрать реализацию в одном месте.',
        'Builder удобен, когда у объекта много параметров и хочется собирать его читаемо и безопасно.',
        'Adapter склеивает несовместимые интерфейсы: например, внешний клиент и внутренний контракт сервиса.',
        'Observer нужен, когда одно событие должно уведомить несколько подписчиков.',
        'Singleton не равен “любой Spring bean”: это паттерн про единственный экземпляр и контролируемый доступ к нему.',
        'Плохой признак — когда паттерн добавили, а код стал тяжелее читать и менять.',
      ],
      interviewFocus: [
        {
          question: 'Какие паттерны проектирования чаще всего встречаются в обычном enterprise-коде?',
          expectedAnswer:
            'Чаще всего называют Builder, Strategy, Factory, Adapter, Observer. Сильный ответ не просто перечисляет названия, а привязывает их к знакомым сценариям: выбор стратегии оплаты, адаптер к внешнему API, builder для DTO/command-объекта.',
        },
        {
          question: 'Когда паттерны вредят?',
          expectedAnswer:
            'Когда их добавляют без реальной задачи: интерфейс ради интерфейса, фабрика для одного конструктора, singleton там, где достаточно обычного объекта. В итоге код становится сложнее без выигрыша.',
        },
      ],
      codeExample: {
        title: 'Strategy вместо if-else по типу скидки',
        language: 'java',
        snippet: `interface DiscountStrategy {
    int apply(int price);
}

class NoDiscountStrategy implements DiscountStrategy {
    public int apply(int price) {
        return price;
    }
}

class VipDiscountStrategy implements DiscountStrategy {
    public int apply(int price) {
        return price - price / 10;
    }
}

class PriceService {
    public int calculate(int basePrice, DiscountStrategy strategy) {
        return strategy.apply(basePrice);
    }
}`,
        walkthrough: [
          'Алгоритм расчёта меняется через стратегию, а не через разрастающийся набор условий внутри одного метода.',
          'Такой код проще тестировать: каждую стратегию можно проверить отдельно.',
        ],
        commonPitfall: 'Путать паттерн с любой “сложной структурой классов” и не понимать, какую именно боль он решает.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Strategy', meaning: 'Паттерн для замены алгоритма или правила без изменения вызывающего кода.' },
        { term: 'Factory Method', meaning: 'Паттерн, который делегирует создание объекта специальному фабричному методу.' },
        { term: 'Builder', meaning: 'Паттерн для пошагового и читаемого создания объекта.' },
        { term: 'Adapter', meaning: 'Паттерн, который согласует несовместимые интерфейсы.' },
        { term: 'Observer', meaning: 'Паттерн подписки: одно событие уведомляет набор подписчиков.' },
        { term: 'Singleton', meaning: 'Паттерн, гарантирующий один экземпляр объекта и общую точку доступа к нему.' },
      ],
      estimatedMinutes: 6,
    }),
    topic({
      id: 'web-network-basics',
      title: 'Базовые веб и сеть: OSI, HTTP-методы, Tomcat и lifecycle сервлета',
      simpleDefinitionOverride:
        'Базовый web/network минимум: модель OSI, идемпотентность HTTP-методов, роль сервлет-контейнера и жизненный цикл сервлета.',
      quickAnswer:
        'OSI — 7-уровневая модель сети; POST и PATCH считаются неидемпотентными; Apache Tomcat — контейнер сервлетов; ключевые этапы сервлета: init, service, destroy.',
      explainBrief: [
        'OSI используют как «карту» при диагностике: на каком уровне проблема — сеть, транспорт, приложение.',
        'Идемпотентность HTTP важна для ретраев: POST/PATCH обычно неидемпотентны, повтор может изменить состояние ещё раз.',
        'В Java EE/Servlet-мире запросы обрабатывает контейнер сервлетов; приложение не работает «само по себе» без контейнера.',
        'Apache Tomcat — самый частый пример сервлет-контейнера для Java web-приложений.',
        'Жизненный цикл сервлета: init (инициализация), service/doGet/doPost (обработка запросов), destroy (освобождение ресурсов).',
      ],
      interviewFocus: [
        {
          question: 'Почему POST и PATCH обычно считают неидемпотентными?',
          expectedAnswer:
            'Повторный вызов может снова изменить состояние: создать ещё одну запись или применить ещё одно частичное изменение.',
        },
        {
          question: 'Что именно делает Tomcat в контексте Java web?',
          expectedAnswer:
            'Поднимает сервлет-контейнер, принимает HTTP-запросы и делегирует их lifecycle-методам сервлетов.',
        },
      ],
      codeExample: {
        title: 'Минимальный сервлет и его жизненный цикл',
        language: 'java',
        snippet: `import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;

public class PingServlet extends HttpServlet {
    @Override
    public void init() {
        // init
    }

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws IOException {
        // service (через doGet)
        resp.getWriter().write("pong");
    }

    @Override
    public void destroy() {
        // destroy
    }
}`,
        walkthrough: [
          'init вызывается один раз при запуске/инициализации сервлета.',
          'doGet/doPost вызываются для каждого входящего запроса.',
          'destroy вызывается при остановке и нужен для cleanup.',
        ],
        commonPitfall: 'Путать Tomcat (сервлет-контейнер) с СУБД, брокером сообщений или web-браузером.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'OSI', meaning: 'Сетевая модель из 7 уровней для описания и диагностики сетевых взаимодействий.' },
        { term: 'Идемпотентность', meaning: 'Повторный вызов операции не меняет итог после первого применения.' },
        { term: 'Servlet container', meaning: 'Среда выполнения Java-сервлетов (например, Apache Tomcat).' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'git-push-pull-fetch',
      title: 'Git: push, pull, fetch и что именно они делают',
      simpleDefinitionOverride:
        '`fetch` забирает изменения с remote, но не меняет рабочую ветку. `pull` обычно делает `fetch` + интеграцию изменений в текущую ветку. `push` отправляет локальные коммиты на remote. Важно понимать разницу, чтобы не обновлять ветку “вслепую”.',
      quickAnswer:
        '`git fetch` безопасно обновляет информацию о remote-ветках. `git pull` ещё и встраивает изменения в текущую ветку. `git push` публикует локальные коммиты. Ошибки часто начинаются, когда `pull` используют автоматически, не понимая, что именно сейчас сольётся.',
      explainBrief: [
        '`fetch` обновляет remote-tracking branches (`origin/main` и т.п.), но рабочее дерево не трогает.',
        '`pull` нужен, когда изменения уже готовы к интеграции в текущую ветку; под капотом это `fetch` и потом merge или rebase.',
        '`push` отправляет только те коммиты, которые есть локально и отсутствуют на remote.',
        'Перед `push` полезно понимать, куда именно пушится ветка и не перезатрёт ли это чужую историю.',
        'Перед `pull` полезно смотреть `git status` и понимать, нет ли незакоммиченных локальных изменений.',
        'Для спокойной работы безопасная привычка такая: сначала `fetch`, потом посмотреть, что изменилось, и только потом решать merge или rebase.',
      ],
      interviewFocus: [
        {
          question: 'В чём разница между `git fetch` и `git pull`?',
          expectedAnswer:
            '`fetch` только скачивает изменения с remote и обновляет ссылки на удалённые ветки. `pull` ещё и пытается встроить эти изменения в текущую локальную ветку, поэтому он более “активный” и может сразу привести к merge/rebase-конфликтам.',
        },
        {
          question: 'Что делает `git push` и почему он может быть отклонён?',
          expectedAnswer:
            '`push` публикует локальные коммиты на remote. Он может быть отклонён, если remote-ветка ушла вперёд, нет прав, не пройдены хуки или локальная история конфликтует с политикой репозитория.',
        },
      ],
      codeExample: {
        title: 'Безопасная последовательность перед обновлением ветки',
        language: 'text',
        snippet: `git status
git fetch origin
git log --oneline HEAD..origin/main
git diff HEAD..origin/main

после этого уже принимают решение:
merge или rebase`,
        walkthrough: [
          'Сначала проверяют локальное состояние и отдельно подтягивают remote-историю.',
          'Только после просмотра новых коммитов решают, как именно встраивать изменения.',
        ],
        commonPitfall: 'Делать `git pull` “на автомате”, не понимая, что под капотом может начаться merge или rebase.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Remote', meaning: 'Удалённый репозиторий, например `origin`.' },
        {
          term: 'Tracking branch',
          meaning:
            'Это локальный "снимок" удалённой ветки: `origin/main` показывает состояние `main` на сервере после `git fetch`, пока ваша `main` ещё не обновлена.',
        },
        { term: 'Upstream', meaning: 'Ветка/remote, с которой связана текущая локальная ветка.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'git-merge-rebase',
      title: 'Слияние веток: merge и rebase',
      simpleDefinitionOverride:
        '`merge` сохраняет историю ветвления и создаёт merge commit, если это нужно. `rebase` переписывает локальные коммиты поверх новой базы и делает историю линейнее. Обе стратегии рабочие, но важно понимать цену и ограничения каждой.',
      quickAnswer:
        '`merge` безопаснее с точки зрения сохранения истории как она была. `rebase` даёт более чистую линейную историю, но переписывает коммиты и требует аккуратности, особенно если ветка уже опубликована и ей пользуются другие.',
      explainBrief: [
        '`merge` просто соединяет две истории; старые SHA коммитов не меняются.',
        '`rebase` как будто “переносит” локальные коммиты на новую базу и создаёт для них новые SHA.',
        'После `rebase` история читается чище: сначала изменения main, потом аккуратная цепочка фичи.',
        'Если ветка уже запушена и её используют другие, rebase может создать путаницу из-за переписывания истории.',
        'Конфликты возможны и в `merge`, и в `rebase`; отличается только форма истории и момент, когда они решаются.',
        'На командном проекте важно не просто знать команды, а следовать согласованной политике репозитория.',
      ],
      interviewFocus: [
        {
          question: 'Когда лучше использовать merge, а когда rebase?',
          expectedAnswer:
            'Merge подходит, когда нужно сохранить естественную историю слияний или не хочется переписывать опубликованные коммиты. Rebase полезен для локальной подготовки ветки перед merge, когда хочется линейной истории и ветка ещё под контролем автора.',
        },
        {
          question: 'Почему rebase опаснее на уже общей ветке?',
          expectedAnswer:
            'Потому что он переписывает коммиты и меняет их SHA. Если другие уже опираются на старую историю, после rebase у команды разъезжаются графы и приходится чинить синхронизацию.',
        },
      ],
      codeExample: {
        title: 'Одна и та же задача двумя способами',
        language: 'text',
        snippet: `merge:
main ----A----B
           \\
feature ----C----D----M

rebase:
main ----A----B----C'----D'`,
        walkthrough: [
          'При merge история ветвления остаётся видимой, а при rebase коммиты фичи “переигрываются” поверх main.',
          'Содержательно результат может быть похож, но история коммитов будет разной.',
        ],
        commonPitfall: 'Говорить, что `rebase` “лучше всегда”. На практике это зависит от политики команды и того, опубликована ветка или нет.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Merge commit', meaning: 'Коммит, который соединяет две линии истории.' },
        { term: 'Rebase', meaning: 'Перенос локальных коммитов на новую базу с переписыванием истории.' },
        { term: 'SHA', meaning: 'Идентификатор коммита в Git.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'git-cherry-pick-squash',
      title: 'Git: cherry-pick и squash',
      simpleDefinitionOverride:
        '`cherry-pick` переносит выбранный коммит из одной ветки в другую. `squash` объединяет несколько коммитов в один. Оба инструмента полезны, когда нужно сделать историю аккуратнее или забрать точечное исправление без полного слияния ветки.',
      quickAnswer:
        '`cherry-pick` нужен для выборочного переноса изменений, например hotfix в release-ветку. `squash` нужен, когда хочется схлопнуть технические промежуточные коммиты и оставить один осмысленный итоговый коммит.',
      explainBrief: [
        '`cherry-pick` полезен, когда нужен один конкретный фикс, а не вся ветка целиком.',
        'Типичный сценарий: баг починили в develop и тот же фикс надо быстро перенести в release или hotfix-ветку.',
        '`squash` делает историю короче и понятнее, если ветка набита коммитами вида “fix”, “more fix”, “oops”.',
        'После `cherry-pick` можно получить конфликт, потому что контекст в целевой ветке уже отличается.',
        '`squash` хорош перед merge в main, если команда предпочитает один содержательный коммит на фичу.',
        'Важно не путать `squash` с потерей истории навсегда: подробная история ещё может жить в feature-ветке или в PR.',
      ],
      interviewFocus: [
        {
          question: 'Когда уместен `git cherry-pick`?',
          expectedAnswer:
            'Когда нужен точечный перенос отдельного коммита: например, срочный hotfix в release-ветку без слияния всей feature-ветки. Важно понимать, что переносится именно эффект выбранного коммита, а не весь контекст ветки.',
        },
        {
          question: 'Зачем делают squash коммитов?',
          expectedAnswer:
            'Чтобы история в main была чище и понятнее: вместо десяти промежуточных коммитов оставить один законченный коммит с нормальным смыслом и сообщением.',
        },
      ],
      codeExample: {
        title: 'Два частых сценария',
        language: 'text',
        snippet: `забрать один фикс:
git cherry-pick <commit-sha>

подготовить ветку к аккуратному merge:
несколько черновых коммитов
-> squash
-> один финальный коммит с понятным смыслом`,
        walkthrough: [
          '`cherry-pick` работает как хирургический перенос одного изменения.',
          '`squash` работает как уборка истории перед публикацией результата.',
        ],
        commonPitfall: 'Использовать `cherry-pick`, не понимая зависимостей коммита от соседних изменений в исходной ветке.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Cherry-pick', meaning: 'Выборочный перенос отдельного коммита в другую ветку.' },
        { term: 'Squash', meaning: 'Объединение нескольких коммитов в один.' },
        { term: 'Hotfix', meaning: 'Срочное исправление бага, которое нужно быстро доставить в нужную ветку.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'git-patch-stash',
      title: 'Git: patch и stash',
      simpleDefinitionOverride:
        '`stash` временно убирает незакоммиченные изменения из рабочей директории, чтобы переключиться на другую задачу. `patch` позволяет работать не со всей разницей целиком, а с выбранными кусками изменений.',
      quickAnswer:
        '`stash` полезен, когда нужно быстро отложить незавершённую работу без коммита. `patch` полезен, когда надо взять или применить только часть изменений: например, кусок diff, отдельный hunk или подготовленный patch-файл.',
      explainBrief: [
        '`git stash` временно складывает текущее состояние и очищает рабочее дерево.',
        'Потом stash можно вернуть (`apply`/`pop`) или удалить, если он больше не нужен.',
        'Это удобно при срочном переключении на багфикс, когда текущая работа ещё не готова к коммиту.',
        '`patch` важен в двух формах: выборочная работа по hunks и перенос изменений через patch-файл.',
        'Patch особенно полезен, когда нужно передать diff вне обычного merge или взять только часть правки.',
        'Термин `hunk` в Git правильный: это отдельный фрагмент diff внутри одного файла, который можно применить выборочно.',
        'Оба инструмента требуют аккуратности: stash легко забыть, а patch можно наложить на неподходящий контекст.',
      ],
      interviewFocus: [
        {
          question: 'Когда полезен `git stash`?',
          expectedAnswer:
            'Когда есть незавершённые локальные изменения, а нужно срочно переключиться на другую ветку или задачу. Stash даёт временно убрать работу без чернового коммита в историю.',
        },
        {
          question: 'Что такое patch в Git и где он бывает нужен?',
          expectedAnswer:
            'Patch — это diff изменений. Он нужен для выборочной работы с кусками правки или для передачи изменений как файла, который потом можно применить в другом месте.',
        },
      ],
      codeExample: {
        title: 'Stash и patch в типовом рабочем сценарии',
        language: 'text',
        snippet: `git status
git stash
git switch hotfix/urgent-bug

после фикса:
git switch feature/old-task
git stash pop

выборочное применение только части изменений (hunks):
git add -p

передача diff как patch-файла:
git diff > fix.patch
git apply fix.patch`,
        walkthrough: [
          'Stash позволяет не тащить незавершённые изменения в чужую задачу.',
          'После возврата важно проверить конфликты и убедиться, что stash применился корректно.',
          '`git add -p` позволяет принять только нужные hunks, а не весь файл целиком.',
          '`git apply` полезен, когда нужно перенести изменения patch-файлом без merge веток.',
        ],
        commonPitfall: 'Накопить несколько stash и забыть, что именно в них лежит и какой из них ещё нужен.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Stash', meaning: 'Временное сохранение незакоммиченных изменений вне истории ветки.' },
        { term: 'Patch', meaning: 'Текст diff-изменений (часто файл `.patch`), который можно передать и применить командой `git apply`.' },
        { term: 'Hunk', meaning: 'Блок изменений в diff внутри одного файла. В patch-режиме (`git add -p`) hunks можно принимать или отклонять по отдельности.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'git-reset-revert',
      title: 'Git: reset и revert',
      simpleDefinitionOverride:
        '`reset` двигает указатель ветки и может менять staging area и рабочую директорию. `revert` не переписывает историю, а создаёт новый коммит, который отменяет изменения старого. Это принципиально разные инструменты.',
      quickAnswer:
        '`reset` чаще используют для локальной переработки незапушенной истории. `revert` используют, когда нужно безопасно отменить уже опубликованный коммит, не ломая общую историю репозитория.',
      explainBrief: [
        'Staging area (index, stage) — это промежуточная зона между рабочими файлами и коммитом: туда попадает именно то, что вы хотите включить в следующий commit.',
        'Как это работает по шагам: меняете файл в working tree -> выбираете нужные изменения через `git add` -> фиксируете только staged-часть командой `git commit`.',
        'Зачем это нужно: можно сделать один логический коммит из части правок в файле, а остальные изменения оставить на потом.',
        'Без staging area Git был бы грубее: коммитился бы весь текущий снимок рабочей директории без удобной выборки кусками.',
        'Именно поэтому команды `git add -p`, `git restore --staged` и режимы reset так важны: они управляют границей между working tree и commit.',
        '`reset --soft` двигает HEAD, но оставляет изменения staged.',
        '`reset --mixed` снимает изменения со stage, но оставляет их в рабочем дереве; это поведение по умолчанию.',
        '`reset --hard` двигает историю и выбрасывает изменения из рабочего дерева; это уже опасная операция.',
        '`revert` создаёт новый коммит-отмену и подходит для общей истории, которую уже видит команда.',
        'Если коммит уже ушёл в remote и его могли забрать другие, `revert` почти всегда безопаснее `reset`.',
        'На собеседовании важно чётко разделять: reset переписывает локальную историю, revert добавляет новую запись об отмене.',
      ],
      interviewFocus: [
        {
          question: 'В чём разница между `git reset` и `git revert`?',
          expectedAnswer:
            '`reset` меняет указатель ветки и может переписать локальную историю, а `revert` создаёт новый коммит, который отменяет изменения выбранного коммита. Поэтому для уже опубликованной истории чаще выбирают `revert`.',
        },
        {
          question: 'Почему `reset --hard` считают опасной командой?',
          expectedAnswer:
            'Потому что он не только двигает HEAD, но и выбрасывает изменения из рабочего дерева и staging area. Если это было нужно сохранить, работа может быть потеряна.',
        },
      ],
      codeExample: {
        title: 'Как staging area связана с reset',
        language: 'text',
        snippet: `схема:
working tree -> git add -> staging area -> git commit -> history

подготовить только часть правок:
git add -p
git status

локально передумали про последний коммит, но staged оставить:
git reset --soft HEAD~1

убрать изменения только из stage, но файлы не терять:
git reset --mixed HEAD~1

в main уже запушили плохой коммит:
git revert <commit-sha>`,
        walkthrough: [
          'Первые команды показывают роль stage как отдельного шага между редактированием и коммитом.',
          '`--soft` и `--mixed` по-разному управляют staging area, поэтому их нельзя путать.',
          '`revert` используют для безопасной отмены в общей ветке без переписывания истории.',
        ],
        commonPitfall: 'Использовать `reset` против уже общей ветки и потом чинить сломанную историю force-push-ем.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'HEAD', meaning: 'Указатель на текущий коммит и текущую позицию в истории.' },
        {
          term: 'Staging area',
          meaning:
            'Промежуточная область (index/stage) между рабочими файлами и историей коммитов. Позволяет отобрать точные изменения для следующего коммита; без неё нельзя было бы удобно собирать коммиты из части правок.',
        },
        { term: 'Reset', meaning: 'Команда для перемещения указателя ветки и, при необходимости, очистки stage/рабочего дерева.' },
        { term: 'Revert', meaning: 'Команда, создающая новый коммит, который отменяет эффект старого.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'build-tools-gradle-maven',
      title: 'Сборщики проектов: Maven и Gradle',
      simpleDefinitionOverride:
        'Сборщик проекта — это инструмент, который описывает, как проект собирать, тестировать, упаковывать и получать зависимости. В Java чаще всего это Maven или Gradle.',
      quickAnswer:
        'И Maven, и Gradle делают одно ядро: собирают проект из исходников в готовый артефакт (обычно jar/war), запускают тесты и подтягивают зависимости. Maven проще стартует новичку из-за более фиксированных правил. Gradle гибче, но требует лучше понимать, как устроены шаги сборки.',
      explainBrief: [
        'Build tool (сборщик) — это инструмент, который по команде выполняет повторяемые шаги: скачать зависимости, скомпилировать код, запустить тесты, собрать артефакт.',
        'Dependency (зависимость) — внешняя библиотека, которую проект использует, например Spring или Jackson.',
        'Jar/war — итоговые файлы сборки: `jar` обычно для библиотек/приложений, `war` для web-приложений в servlet-контейнере.',
        'Plugin (плагин) — расширение сборщика, которое добавляет шаги, например проверку стиля, покрытие тестами или публикацию артефактов.',
        'Maven основан на lifecycle (фиксированная цепочка фаз: validate -> compile -> test -> package ...), обычно описывается в `pom.xml`.',
        'Gradle основан на task-модели: сборка — это набор задач (`task`), которые можно гибко комбинировать в `build.gradle`/`build.gradle.kts`.',
        'DSL — специальный язык конфигурации сборки. В Gradle это скрипт, где вы описываете зависимости и задачи более программно.',
        'Для новичка практично так: сначала уверенно освоить Maven (проще предсказуемость), потом Gradle (больше гибкости для сложных сценариев).',
        'На собеседовании сильный ответ: не спорить “что лучше”, а объяснить, чем отличаются подходы и когда команда выбирает каждый.',
      ],
      interviewFocus: [
        {
          question: 'В чём базовая разница между Maven и Gradle?',
          expectedAnswer:
            'Maven опирается на стандартный lifecycle и более декларативный подход, поэтому его конфигурация часто предсказуемее. Gradle гибче и строится вокруг задач, что удобно для сложных или нестандартных сборочных сценариев.',
        },
        {
          question: 'Что делает сборщик проекта кроме “собрать jar”?',
          expectedAnswer:
            'Он управляет зависимостями, запускает тесты, подключает плагины, прогоняет проверки качества, собирает артефакты и стандартизирует процесс сборки для всей команды и CI.',
        },
      ],
      codeExample: {
        title: 'Как обычно выглядит роль сборщика',
        language: 'text',
        snippet: `исходники
-> зависимости
-> компиляция
-> тесты
-> packaging
-> артефакт для запуска или публикации`,
        walkthrough: [
          'Сборщик нужен, чтобы эта цепочка была воспроизводимой у разработчика, на CI и на других машинах.',
          'Без сборщика проект быстро скатывается в ручные шаги и разъезжающиеся окружения.',
        ],
        commonPitfall: 'Сводить Maven/Gradle только к загрузке библиотек и не видеть их роли в общем процессе сборки.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        {
          term: 'Build tool',
          meaning:
            'Инструмент, который автоматизирует полный конвейер: зависимости, компиляция, тесты, упаковка и публикация артефактов. Без него сборка превращается в ручные шаги и нестабильный результат на разных машинах.',
        },
        {
          term: 'Dependency',
          meaning: 'Внешняя библиотека, от которой зависит проект и которую сборщик подтягивает по координатам.',
        },
        { term: 'Lifecycle', meaning: 'Фиксированная последовательность фаз сборки (особенно в Maven).' },
        { term: 'Plugin', meaning: 'Подключаемый модуль сборщика, добавляющий отдельный шаг или проверку.' },
        { term: 'DSL', meaning: 'Язык конфигурации предметной области; в Gradle им описывают задачи и зависимости.' },
        { term: 'Artifact', meaning: 'Итог сборки, например `jar`, `war`, `pom` и сопутствующие файлы.' },
        { term: 'pom.xml', meaning: 'Основной конфигурационный файл Maven-проекта.' },
        { term: 'Task', meaning: 'Отдельная задача в Gradle, например compile, test или publish.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'maven-lifecycle',
      title: 'Этапы сборки в Maven: что за чем идёт',
      simpleDefinitionOverride:
        'Maven lifecycle — это стандартная цепочка фаз сборки. Каждая следующая фаза включает предыдущие, поэтому команда вроде `mvn package` запускает не только упаковку, но и компиляцию, тесты и более ранние шаги.',
      quickAnswer:
        'Чаще всего в разговоре про Maven нужны фазы `validate`, `compile`, `test`, `package`, `verify`, `install`, `deploy`. Важно понимать не только названия, но и зачем команда реально запускает ту или иную фазу.',
      explainBrief: [
        '`validate` проверяет корректность `pom.xml`, обязательные параметры и доступность нужной конфигурации сборки.',
        '`compile` собирает только основной код (`src/main`), поэтому на этом шаге видно базовую валидность бизнес-кода.',
        '`test` компилирует и запускает тесты (`src/test`), чтобы поймать регрессии до упаковки артефакта.',
        '`package` формирует итоговый jar/war. Обычно эту фазу запускают перед локальной проверкой запуска приложения.',
        '`verify` прогоняет дополнительные quality-gates после упаковки: интеграционные проверки, правила плагинов, smoke-проверки.',
        '`install` кладёт артефакт в локальный `.m2`, чтобы соседние локальные модули/проекты могли брать его как зависимость.',
        '`deploy` публикует артефакт в удалённый репозиторий (Nexus/Artifactory) для команды, CI и окружений.',
        'Практика: локально чаще `mvn test` или `mvn package`, а `deploy` обычно выполняется в CI после всех проверок.',
      ],
      interviewFocus: [
        {
          question: 'Что произойдёт при `mvn package`?',
          expectedAnswer:
            'Запустятся все более ранние фазы lifecycle до `package`: проверка проекта, компиляция, тесты, а затем упаковка артефакта. То есть `package` не работает изолированно сам по себе.',
        },
        {
          question: 'Чем `install` отличается от `deploy`?',
          expectedAnswer:
            '`install` публикует артефакт только в локальный репозиторий на машине разработчика. `deploy` отправляет его в удалённый репозиторий, доступный другим машинам и CI.',
        },
      ],
      codeExample: {
        title: 'Упрощённая цепочка lifecycle',
        language: 'text',
        snippet: `validate
-> compile
-> test
-> package
-> verify
-> install
-> deploy`,
        walkthrough: [
          'Команда запускает выбранную фазу и всё, что стоит до неё в lifecycle.',
          'Поэтому чем “правее” фаза, тем больше шагов реально отработает Maven.',
        ],
        commonPitfall: 'Думать, что `package` или `install` — это одиночные действия без запуска предыдущих фаз.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Lifecycle', meaning: 'Стандартная последовательность фаз сборки Maven.' },
        { term: 'Phase', meaning: 'Отдельный этап lifecycle, например `compile`, `test` или `package`.' },
        { term: 'Artifact', meaning: 'Результат сборки: jar, war, pom и связанные файлы.' },
        { term: 'Deploy', meaning: 'Публикация артефакта в удалённый репозиторий.' },
      ],
      estimatedMinutes: 5,
    }),
    topic({
      id: 'maven-local-repository',
      title: 'Где на локальной машине лежат скачанные зависимости',
      simpleDefinitionOverride:
        'Maven хранит скачанные зависимости в локальном репозитории пользователя. По умолчанию это каталог `.m2/repository`, где лежат jar-файлы, pom-файлы и метаданные по координатам артефактов.',
      quickAnswer:
        'На локальной машине Maven обычно складывает зависимости в `~/.m2/repository` на Linux/macOS или в аналогичный каталог профиля пользователя на Windows. Там артефакты разложены по `groupId/artifactId/version`.',
      explainBrief: [
        'Локальный репозиторий нужен, чтобы не скачивать одну и ту же зависимость заново при каждой сборке.',
        'Структура каталогов отражает координаты артефакта: groupId, artifactId, version.',
        'Там лежат не только jar, но и `pom`, checksum и другие служебные файлы.',
        'Если зависимость повреждена или скачалась криво, иногда чистят конкретную папку, а не весь репозиторий целиком.',
        'Путь к локальному репозиторию можно переопределить в настройках Maven.',
        'Gradle хранит кэш зависимостей отдельно, поэтому не стоит путать Maven local repo и Gradle cache.',
      ],
      interviewFocus: [
        {
          question: 'Где Maven хранит скачанные зависимости локально?',
          expectedAnswer:
            'Обычно в локальном репозитории `.m2/repository` внутри домашнего каталога пользователя. На Windows это каталог внутри профиля пользователя, на Linux/macOS — в домашней директории.',
        },
        {
          question: 'Зачем знать про локальный репозиторий на практике?',
          expectedAnswer:
            'Это помогает разбирать проблемы со скачиванием зависимостей, конфликтами версий, офлайн-сборкой и повреждённым кэшем. Часто достаточно удалить одну папку артефакта и пересобрать проект.',
        },
      ],
      codeExample: {
        title: 'Как выглядит путь по координатам артефакта',
        language: 'text',
        snippet: `dependency:
groupId    = org.springframework
artifactId = spring-core
version    = 5.3.30

путь в local repo:
.m2/repository/org/springframework/spring-core/5.3.30/`,
        walkthrough: [
          'Каталоги повторяют координаты зависимости, поэтому нужный артефакт легко найти руками.',
          'По этой же структуре проще понимать, что именно уже скачано и какая версия используется локально.',
        ],
        commonPitfall: 'Удалять весь `.m2/repository` при любой проблеме, хотя часто достаточно почистить только один конфликтный артефакт.',
        productionNote:
          'На CI локальный кэш зависимостей тоже важен: он ускоряет сборки, но при неправильной настройке может тащить старые или повреждённые артефакты.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: '.m2/repository', meaning: 'Локальный Maven-репозиторий с зависимостями и артефактами.' },
        { term: 'groupId', meaning: 'Группа артефакта, обычно отражающая организацию или пространство имён.' },
        { term: 'artifactId', meaning: 'Имя конкретного артефакта внутри группы.' },
      ],
      estimatedMinutes: 4,
    }),
    topic({
      id: 'homework-3-patterns',
      title: 'Домашнее задание 3: эталон по паттернам (Strategy, Chain, Builder, Proxy, Decorator, Adapter)',
      simpleDefinitionOverride:
        'Эталон ДЗ-3: в одном месте показать рабочие реализации всех 6 паттернов с понятным разделением, где какой паттерн применяется и какую проблему решает.',
      quickAnswer:
        'Правильный ответ по ДЗ-3 — не просто "написать классы", а явно показать каждый паттерн в коде и пояснить, какую практическую задачу он закрывает.',
      explainBrief: [
        'Strategy: замена алгоритма без переписывания вызывающего кода.',
        'Chain of Responsibility: последовательная обработка запроса несколькими обработчиками.',
        'Builder: безопасное создание объекта с множеством параметров.',
        'Proxy: контроль доступа/логирования/кэширования поверх реального объекта.',
        'Decorator: динамическое расширение поведения без наследования.',
        'Adapter: согласование несовместимых интерфейсов.',
        'Для зачёта домашки важно, чтобы в коде было явно видно, где начинается и заканчивается каждый паттерн.',
      ],
      interviewFocus: [
        {
          question: 'Как быстро проверить, что все 6 паттернов действительно реализованы, а не названы в комментариях?',
          expectedAnswer:
            'По наличию ключевых ролей паттерна в коде: Strategy-интерфейс и реализации, цепочка обработчиков, Builder с build(), Proxy-обёртка, Decorator-обёртка, Adapter между несовместимыми API.',
        },
      ],
      codeExample: {
        title: 'Компактный эталон ДЗ-3: все 6 паттернов',
        language: 'java',
        snippet: `import java.util.ArrayList;
import java.util.List;

public class Homework3Patterns {
    public static void main(String[] args) {
        PaymentService paymentService = new PaymentService(new CardPaymentStrategy());
        paymentService.pay(1000);
        paymentService.setStrategy(new CashPaymentStrategy());
        paymentService.pay(500);

        Handler chain = new AuthHandler(new ValidationHandler(new BusinessHandler(null)));
        chain.handle("request");

        User user = User.builder().id(1L).name("Yura").role("student").build();
        System.out.println(user);

        Storage storage = new StorageProxy(new FileStorage());
        storage.save("report");

        Notification notification = new SmsDecorator(new EmailNotification());
        notification.send("Hello");

        LegacyClient legacyClient = new LegacyClient();
        NewClient adapter = new LegacyClientAdapter(legacyClient);
        adapter.sendMessage("adapter message");
    }
}

interface PaymentStrategy {
    void pay(int amount);
}

class CardPaymentStrategy implements PaymentStrategy {
    public void pay(int amount) {
        System.out.println("Card payment: " + amount);
    }
}

class CashPaymentStrategy implements PaymentStrategy {
    public void pay(int amount) {
        System.out.println("Cash payment: " + amount);
    }
}

class PaymentService {
    private PaymentStrategy strategy;

    PaymentService(PaymentStrategy strategy) {
        this.strategy = strategy;
    }

    void setStrategy(PaymentStrategy strategy) {
        this.strategy = strategy;
    }

    void pay(int amount) {
        strategy.pay(amount);
    }
}

abstract class Handler {
    private final Handler next;

    Handler(Handler next) {
        this.next = next;
    }

    void handle(String request) {
        process(request);
        if (next != null) {
            next.handle(request);
        }
    }

    abstract void process(String request);
}

class AuthHandler extends Handler {
    AuthHandler(Handler next) { super(next); }
    void process(String request) { System.out.println("Auth ok: " + request); }
}

class ValidationHandler extends Handler {
    ValidationHandler(Handler next) { super(next); }
    void process(String request) { System.out.println("Validation ok: " + request); }
}

class BusinessHandler extends Handler {
    BusinessHandler(Handler next) { super(next); }
    void process(String request) { System.out.println("Business done: " + request); }
}

class User {
    private final Long id;
    private final String name;
    private final String role;

    private User(Builder builder) {
        this.id = builder.id;
        this.name = builder.name;
        this.role = builder.role;
    }

    static Builder builder() { return new Builder(); }

    static class Builder {
        private Long id;
        private String name;
        private String role;
        Builder id(Long id) { this.id = id; return this; }
        Builder name(String name) { this.name = name; return this; }
        Builder role(String role) { this.role = role; return this; }
        User build() { return new User(this); }
    }
}

interface Storage {
    void save(String data);
}

class FileStorage implements Storage {
    public void save(String data) { System.out.println("Saved: " + data); }
}

class StorageProxy implements Storage {
    private final Storage target;
    StorageProxy(Storage target) { this.target = target; }
    public void save(String data) {
        System.out.println("Proxy before");
        target.save(data);
        System.out.println("Proxy after");
    }
}

interface Notification {
    void send(String message);
}

class EmailNotification implements Notification {
    public void send(String message) { System.out.println("Email: " + message); }
}

abstract class NotificationDecorator implements Notification {
    protected final Notification delegate;
    NotificationDecorator(Notification delegate) { this.delegate = delegate; }
}

class SmsDecorator extends NotificationDecorator {
    SmsDecorator(Notification delegate) { super(delegate); }
    public void send(String message) {
        delegate.send(message);
        System.out.println("SMS: " + message);
    }
}

class LegacyClient {
    void sendLegacy(String payload) { System.out.println("Legacy: " + payload); }
}

interface NewClient {
    void sendMessage(String message);
}

class LegacyClientAdapter implements NewClient {
    private final LegacyClient legacyClient;
    LegacyClientAdapter(LegacyClient legacyClient) { this.legacyClient = legacyClient; }
    public void sendMessage(String message) { legacyClient.sendLegacy(message); }
}`,
        walkthrough: [
          'Каждый паттерн выделен отдельной группой классов, чтобы проверяющий видел 1:1 соответствие заданию.',
          'В `main` есть вызов для каждого паттерна — можно быстро показать работоспособность.',
          'Структура кода ориентирована на проверку домашки: легко показать "тут Strategy, тут Adapter".',
        ],
        commonPitfall: 'Смешать паттерны в один “монолитный” пример, где невозможно понять, что именно реализовано.',
        productionNote:
          'В реальном проекте паттерны обычно распределены по слоям и use-case, но для домашнего задания лучше явная демонстрация каждого отдельно.',
      },
      usefulLinksOverride: MODULE3_LINKS,
      glossary: [
        { term: 'Strategy', meaning: 'Подмена алгоритма через общий интерфейс.' },
        { term: 'Decorator', meaning: 'Расширение поведения через обёртку над объектом.' },
        { term: 'Adapter', meaning: 'Преобразование несовместимого интерфейса в нужный контракт.' },
      ],
      estimatedMinutes: 8,
    }),
  ],
};
